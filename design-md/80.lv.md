# How 80.lv is designed

[Open the live Fudge conversation](https://design.withfudge.com/share/80.lv-design)

Last updated: 2026-08-03

## Captured pages

[![80 Level](https://pin.fontofweb.com/9652?format=jpg)](https://design.withfudge.com/share/pin-9652)

[80 Level](https://design.withfudge.com/share/pin-9652)

[![80 Level](https://pin.fontofweb.com/9651?format=jpg)](https://design.withfudge.com/share/pin-9651)

[80 Level](https://design.withfudge.com/share/pin-9651)

[![80 Level](https://pin.fontofweb.com/9650?format=jpg)](https://design.withfudge.com/share/pin-9650)

[80 Level](https://design.withfudge.com/share/pin-9650)

## Overview

80.lv presents a predominantly light surface treatment in the captured pages. The system is anchored by `#474b2f`, `#f9fbf7`, and `#ffffff`, with typography led by **Euclid Circular B** and **Applesystem**. The guide below names reusable design roles only where the captured evidence supports them.

The reference set uses representative captured pages. It is a design reference, not a claim about the site's private source code or a complete responsive specification.

## Design tokens

The token names are practical labels for the recorded values. They are not claims about the source site's variable names.

### Colors

| token | value | observed use |
| --- | --- | --- |
| color.text | `#474b2f` | text on page content |
| color.surface | `#f9fbf7` | surface on page content |
| color.surface | `#ffffff` | surface on page content in the header |
| color.text | `#000000` | text on page content |
| color.surface | `#f4f4f1` | surface on page content in the main |
| color.shadow | `#c6c6c6` | shadow on page content |
| color.text | `#484b32` | text on page content in the main |
| color.text | `#a1a490` | text on page content in the main |
| color.text | `#a1a492` | text on page content in the main |
| color.text | `#9ea5ad` | text on page content in the main |
| color.text | `#1a1d1f` | text on page content in the main |
| color.border | `#eceee4` | border on page content in the main |

### Typography

| token | font family | size | weight | line height | letter spacing | observed use |
| --- | --- | --- | --- | --- | --- | --- |
| type.body-15px | **Euclid Circular B** | 15px | 400 | 19.995px | normal | <body>, <div> in the header, <header> in the header, <div>, <div> in the main, <section> in the main, <main> in the main, <a> in the main, <html>, <img> in the main, <article> in the main, <p> in the main, <ul> in the main, <img> in the aside, <div> in the aside, <a> in the aside, <aside> in the aside, and <ul> in the aside |
| type.heading-31px | **Euclid Circular B** | 31px | 600 | 42.16px | normal | <h2> in the main |
| type.heading-15px | **Euclid Circular B** | 15px | 600 | 20px | normal | <a> in the aside, <li> in the aside, and <div> in the aside |
| type.body-15px | **Euclid Circular B** | 15px | 400 | 20px | normal | <div> in the main |
| type.body-14px | **Applesystem** | 14px | 400 | normal | normal | <div> |
| type.display-32px | **Euclid Circular B** | 32px | 600 | 44px | normal | <a> in the main |
| type.body-16px | **Euclid Circular B** | 16px | 300 | 21.328px | normal | <div> in the header |
| type.body-16px | **Euclid Circular B** | 16px | 400 | 22px | -0.16px | <p> in the main |
| type.heading-18px | **Euclid Circular B** | 18px | 600 | 24px | normal | <h3> |
| type.utility-13px | **Euclid Circular B** | 13px | 400 | 20px | normal | <a> in the main and <li> in the main |
| type.body-14px | **Euclid Circular B** | 14px | 400 | 18px | normal | <label> in the main and <label> in the aside |
| type.body-16px | **Euclid Circular B** | 16px | 500 | 21.328px | normal | <div> in the header |
| type.heading-20px | **Euclid Circular B** | 20px | 600 | 28px | normal | <h5> in the main and <h5> in the aside |
| type.heading-15px | **Euclid Circular B** | 15px | 600 | 19.995px | normal | <button> in the button |
| type.display-32px | **Euclid Circular B** | 32px | 600 | 44px | -0.48px | <h1> in the main |
| type.heading-16px | **Euclid Circular B** | 16px | 700 | 22px | -0.16px | <a> in the main |

### Spacing

| value | property | observed element |
| --- | --- | --- |
| `0px 0px 60px` | margin | page content |
| `60px` | margin-bottom | page content |
| `48px` | padding-left | page content |
| `0px 48px` | padding | page content |
| `48px` | padding-right | page content |
| `72px 0px 0px` | padding | page content |
| `72px` | padding-top | page content |
| `0px 0px 60px` | padding | page content |

### Shape and depth

The captured pages use `0px none 20px`, `0px none 16px`, `0px none 0px 0px 20px 20px`, `0px none 20px 20px 0px 0px`, `0px none 50%`, `2px solid 22px`, and `2px solid 50%` border treatments. Recorded gradients include `linear-gradient(rgba(0, 0, 0, 0) 0%, rgb(17, 17, 19) 100%)`. Recorded shadows include `rgba(0, 0, 0, 0.08) 0px 1px 3px 0px` and `rgba(198, 198, 198, 0.55) 0px 12px 39px 0px`.

## Colors

The visual system is predominantly light surface treatment. Surface colors include `#f9fbf7`, `#ffffff`, and `#f4f4f1`. Text colors include `#474b2f`, `#000000`, and `#484b32`. Keep image-derived colors separate from interface colors.

## Typography

The captured type system uses **Euclid Circular B** and **Applesystem**. The table records measured size, weight, leading, and tracking where available; it does not establish licensing or a complete fallback stack.

## Layout

The captured pages use values such as `0px 0px 60px`, `60px`, `48px`, `0px 48px`, and `48px` for padding and margin. These are measured usages, not a resolved scale. The available evidence does not establish container widths, grid rules, or breakpoints.

## Visual language

The captured visual language is predominantly light surface treatment. Surface treatment centers on `#f9fbf7`, `#ffffff`, `#f4f4f1`, and `#f1f2f2`. Depth is supported by the returned gradients and shadows.

## Components and states

The captured pages include repeated styling on <div>, <body>, <header>, <section>, <main>, <a>, <p>, and <label> elements. The evidence does not identify reusable component boundaries or interaction states. Treat loading, focus, hover, pressed, error, and disabled states as implementation work to validate rather than as captured facts.

## Responsive behavior

The captured token set does not establish breakpoint values or mobile behavior. Validate stacking, navigation changes, image treatment, and type scaling against the representative captures before implementing a responsive adaptation.

## Practical implementation guidance

- Preserve the light surface relationship and the measured type hierarchy before adding decorative treatment.
- Start from the returned typography, spacing, color, and border values; name any new scale or semantic role as an adaptation rather than as a source fact.
- Keep the captured font families separate from licensing decisions.
- Use screenshots to validate component anatomy, responsive behavior, focus treatment, and motion before shipping.
- Avoid introducing gradients or shadows beyond the captured treatment; the returned evidence includes those treatments.

## Scope note

The guide uses representative captured pages. The evidence supports the values and observations above; it does not establish component semantics, interaction states, motion timing, breakpoint rules, font licensing, or a complete source stylesheet.

## Colors

- `#474b2f`
- `#f9fbf7`
- `#ffffff`
- `#000000`
- `#f4f4f1`
- `#c6c6c6`
- `#484b32`
- `#a1a490`
- `#a1a492`
- `#9ea5ad`
- `#1a1d1f`
- `#eceee4`
- `#f1f2f2`
- `#393935`

## Typography captured from 80.lv

- **Euclid Circular B** — weight 400 · size 15px
- **Applesystem** — weight 400 · size 14px
